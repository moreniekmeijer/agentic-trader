[project]
name = "agentic-trader"
version = "0.1.0"
description = "Agentic trading system with Alpaca"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "sqlalchemy",
    "psycopg2-binary",
    "uvicorn",
    "apscheduler",
    "alpaca-py",
    "pandas",
    "pytz",
    "yfinance",
    "pydantic",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "ty",
]

# ------------------------
# RUFF
# ------------------------
[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]  # errors, flakes, imports
ignore = []

[tool.ruff.format]
quote-style = "double"

# ------------------------
# PYTEST
# ------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

# ------------------------
# TYPE CHECKING (ty)
# ------------------------
