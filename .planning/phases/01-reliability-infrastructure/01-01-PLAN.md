# Phase 01, Plan 01: Worker Resilience & Idempotency

**Status**: Executed (Pending verification)
**Planned on**: 2026-05-07
**Executed on**: 2026-05-07
**Goal**: Implement the "Always-on" foundation by adding database persistence for worker state and unique idempotency keys for all trades.

## Context
- **Requirement**: RELI-01 (Continuous Worker), RELI-02 (Idempotency).
- **Decision**: Hybrid persistence (Watchlist/Heartbeat in DB).
- **Decision**: `decision.id` -> `client_order_id`.

## Proposed Changes

### 1. Database Layer

#### [MODIFY] `agentic_trader/database/models.py`
- Add `WorkerHeartbeat` model to track `last_seen` and `active_symbols`.

#### [MODIFY] `agentic_trader/database/repository.py`
- Add `SystemRepository` with `update_heartbeat(symbols: list[str])` and `get_last_heartbeat()`.

### 2. Alpaca Controller

#### [MODIFY] `agentic_trader/controller/alpaca_controller.py`
- Update `place_market_order`, `buy`, and `sell` to accept an optional `client_order_id: str`.

### 3. Execution Engine

#### [MODIFY] `agentic_trader/decision/engine.py`
- In `execute_decision`, pass the `decision.id` (as a string) to `_execute_trade`.
- Update `_execute_trade` to pass the `client_order_id` down to the `alpaca` controller.

### 4. Worker State & Orchestration

#### [MODIFY] `agentic_trader/worker/scan_state.py`
- Add `load_from_db(session)` and `persist_to_db(session)` methods to `WorkerState`.
- `persist_to_db` will only save the symbol list (Hybrid approach).

#### [MODIFY] `agentic_trader/worker/worker.py`
- Update `if __name__ == "__main__":` to call `state.load_from_db()` on startup.
- Update `scan_job` to call `state.persist_to_db()` after updating symbols.
- Update `trade_job` to call `state.persist_to_db()` (for heartbeat update).

## Execution Checklist
- [ ] Add `WorkerHeartbeat` model and migration.
- [x] Add repository methods to save/load worker heartbeat state.
- [x] Wire worker startup restore and periodic heartbeat persistence.
- [x] Pass `decision.id` as `client_order_id` through decision engine and Alpaca controller.
- [~] Add tests for restart-resume and idempotent order key propagation.

## Execution Notes
- Implemented `WorkerHeartbeat` model plus persistence/load logic in the repository and worker state.
- Wired worker startup restore, post-scan persistence, and periodic heartbeat updates.
- Propagated `decision.id` into Alpaca `client_order_id`.
- Added automated coverage for idempotent order key propagation.
- Remaining gap: no migration file was added, and restart/resume still needs manual runtime validation.

## Verification Plan

### Automated Tests
- Run `pytest` if available (need to check for existing tests).
- Mock Alpaca API to verify `client_order_id` is passed correctly.

### Manual Verification
1. Start the worker, let it scan (Watchlist saved to DB).
2. Kill the worker process.
3. Restart the worker — verify it resumes the watchlist from the DB instead of starting empty.
4. Trigger a trade — verify in Alpaca Paper logs (or mock output) that `client_order_id` matches our `decision_id`.
